name: NAME
version: 0.0.0

dependencies:
- base >= 4.7 && < 5
- aeson
- pcre-light
- serverless-haskell

executables:
  main:
    main: Main.hs
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
